<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <TextView
        android:id="@+id/definition_number"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintWidth_percent=".1"
        android:layout_height="wrap_content"
        android:layout_width="0dp"
        style="@style/definition_tile"
        android:textAlignment="viewEnd"
        android:paddingEnd="8dp"
        android:paddingStart="8dp"/>

    <TextView
        android:id="@+id/definition_textview"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/definition_number"
        app:layout_constraintEnd_toStartOf="@id/is_enabled_checkbox"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        style="@style/definition_tile"/>

    <CheckBox
        android:id="@+id/is_enabled_checkbox"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAlignment="viewEnd"/>

</androidx.constraintlayout.widget.ConstraintLayout>